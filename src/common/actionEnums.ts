const myModule = "[MYMODULE]"

export const actionEnums = {
    UPDATE_USER_PROFILE_NAME: `${myModule}[0] Updates the user profile name`,
    MEMBER_REQUEST_COMPLETED: `${myModule}[1] Results from member request res api call`,
}